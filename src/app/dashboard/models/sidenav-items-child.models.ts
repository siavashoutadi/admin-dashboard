export class SideNavMenuItemChild {
    public id: string;
    public title: string;
    public route: string;

    constructor(id: string, title: string, route: string) {
        this.id = id;
        this.title = title;
        this.route = route;
    }
}
