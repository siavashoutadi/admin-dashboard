export class SideNavMenuItemChild {
    public title: string;
    public route: string;

    constructor(title: string, route: string) {
        this.title = title;
        this.route = route;
    }
}
